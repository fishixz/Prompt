{
  "name": "rocha-ticket-bot",
  "version": "1.0.0",
  "description": "Sistema completo e configurável de tickets para o Rocha Roleplay, compatível com Termux.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && find src -name '*.js' -type f -print0 | xargs -0 -n1 node --check"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "discord.js": "^14.27.0",
    "dotenv": "^17.4.2"
  }
}
