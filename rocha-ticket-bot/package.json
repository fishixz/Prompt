{
  "name": "rocha-ticket-bot",
  "version": "1.2.0",
  "description": "Sistema avançado, configurável e resiliente de tickets para o Rocha Roleplay, compatível com Termux.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "selftest": "node src/tests/selfTest.js",
    "check": "find src -name '*.js' -type f -print0 | xargs -0 -n1 node --check && npm run selftest"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "discord.js": "^14.27.0",
    "dotenv": "^17.4.2"
  }
}
